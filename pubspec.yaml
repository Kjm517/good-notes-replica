name: notably
description: "Take notes comfortably. Think notably — handwriting, PDFs, and quizzes on every device."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.11.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.6.1
  go_router: ^17.3.0
  drift: ^2.34.3
  drift_flutter: ^0.3.1
  # sqlite3 3.x ships native libs via Dart hooks; do not use
  # sqlite3_flutter_libs (0.6+eol is empty and leaves Android without libsqlite3.so).
  sqlite3: ^3.5.0
  path_provider: ^2.1.6
  path: ^1.9.1
  perfect_freehand: ^2.5.2+1
  uuid: ^4.6.0
  collection: ^1.19.1
  google_fonts: ^8.2.0
  shared_preferences: ^2.5.5
  shared_preferences_web: ^2.4.3
  intl: ^0.20.3
  file_picker: ^11.0.3
  pdfx: ^2.9.2
  pdf: ^3.12.0
  printing: ^5.14.3
  crypto: ^3.0.7
  supabase_flutter: ^2.9.1
  http: ^1.6.0
  syncfusion_flutter_pdf: ^33.2.13
  image_picker: ^1.2.3
  google_generative_ai: ^0.4.6
  flutter_dotenv: ^6.0.1
  connectivity_plus: ^7.3.1
  pdfrx: ^2.4.7
  url_launcher: ^6.3.2
  web: ^1.1.1
  purchases_flutter: ^10.9.1
  purchases_ui_flutter: ^10.9.1
  share_plus: ^12.0.2
  # Runtime preserve/remove so the native splash stays until Flutter is ready
  # (avoids a second, different splash flash). CLI generators stay in
  # dev_dependencies below.
  flutter_native_splash: ^2.4.7
  qr_flutter: ^4.1.0
  app_links: ^7.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  drift_dev: ^2.34.0
  build_runner: ^2.15.1
  flutter_launcher_icons: ^0.14.4
  flutter_native_splash: ^2.4.7

flutter_launcher_icons:
  android: true
  ios: true
  web:
    generate: true
    image_path: assets/branding/app_icon_1024.png
    background_color: "#5759D3"
  # Square crop of assets/branding/app_icon.png (source is letterboxed).
  image_path: assets/branding/app_icon_1024.png
  # Full-bleed plate — match brand periwinkle so adaptive edges blend.
  adaptive_icon_background: "#5759D3"
  adaptive_icon_foreground: assets/branding/app_icon_1024.png
  adaptive_icon_foreground_inset: 0
  remove_alpha_ios: true

flutter_native_splash:
  color: "#000000"
  color_dark: "#000000"
  # Padded square, not the bare wordmark. The generated iOS storyboard pins the
  # image view to all four edges with scaleAspectFit, so whatever is handed to
  # it is scaled up until it touches the window — a bare 1057x400 logo came out
  # spanning the full width of an iPad. The margins have to be baked into the
  # asset; a square canvas keeps them even in both orientations.
  image: assets/branding/notably_logo_splash_padded.png
  android_12:
    # Same art as the main splash — never the launcher/app icon.
    color: "#000000"
    color_dark: "#000000"
    image: assets/branding/notably_logo_splash.png
    icon_background_color: "#000000"
    icon_background_color_dark: "#000000"
  android: true
  ios: true
  web: false

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  config:
    # Xcode 16/26 crashes resolving plugin SPM graphs
    # (insertObjects:atIndexes count mismatch). CocoaPods is stable here.
    enable-swift-package-manager: false

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - .env
    - assets/env
    - assets/quiz/stop_words.txt
    - assets/branding/notably_logo.png
    - assets/branding/notably_logo_splash.png
    - assets/branding/notably-solo-logo.png
    - assets/branding/app_icon.png
    - assets/branding/app_icon_1024.png
    - assets/payments/gcash.png
    - assets/payments/maya.png

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
